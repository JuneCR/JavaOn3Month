# 前后端学习笔记(2026.9 - 2026.12)



## 一、JavaWeb知识回顾

> 笔记来源:D:\计算机\Java\我的笔记本\JAVA.on

### 	1.github更新内容命令

​        	cd 文件目录

​                git add .

​                git commit -m "修改内容"

​                git push

### 	2.SSM框架复习回顾

####                Spring6

#####               		SpringIoc

​		**原理**: ioc负责存储对象(声明) ,di负责该对象内的参数

​		**依赖**：spring-context

##### 		<1> **xml配置ioc**

###### 		无参构造函数实例化组件 

```
public class HappyComponent {
    //默认包含无参数构造函数
    public void doWork() {
        System.out.println("HappyComponent.doWork");
    }
}
--------------------------------------------------------
<bean id="唯一的标识" class="类的权限限定符" scope="singleton单例/prototype多例">
<bean id="happyComponent1" class="com.june.ioc_01.HappyComponent"/>
<bean id="happyComponent2" class="com.june.ioc_01.HappyComponent"/>
```

###### 		有参构造函数实例化组件 

```
public class UserDao {
}

package com.june.ioc_02;
public class UserService {
    
    private UserDao userDao;

    public UserService(UserDao userDao) {
        this.userDao = userDao;
    }

    private int age;

    private String name;

    public UserService(int age , String name ,UserDao userDao) {
        this.userDao = userDao;
        this.age = age;
        this.name = name;
    }
}
package com.june.ioc_02;

public class MovieFinder {

}
package com.june.ioc_02;

public class SimpleMovieLister {

    private MovieFinder movieFinder;

    private String movieName;

    public void setMovieFinder(MovieFinder movieFinder) {
        this.movieFinder = movieFinder;
    }

    public void setMovieName(String movieName){
        this.movieName = movieName;
    }

    // business logic that actually uses the injected MovieFinder is omitted...
}
-----------------------------------------------------------------------
<bean id="userDao" class="com.june.ioc_02.UserDao"/>
<bean id="userService" class="com.june.ioc_02.UserService">
	/*
		指定参数传值 di配置
		constructor-arg 构造参数传值的 di配置
		value = 直接属性值
		ref = 引用其他的bean
	*/
	/*
		声明规范: 
		1.按照构造参数的顺序填写
		public UserService(int age , String name ,UserDao userDao)
		2.使用constructor name=""
		3.按照构造参数的下标填写
		public UserService(int age , String name ,UserDao userDao)
	*/
	//1
	<constructor-arg value="18"/>
	<constructor-arg value="二狗子"/>
	<constructor-arg ref="userDao"/>
	//2
	<constructor-arg name="name" value="二狗子"/>
    <constructor-arg name="age" value="18"/>
    <constructor-arg name="userDao" value="userDao"/>
	//3
	<constructor-arg index="1" value="二狗子"/>
    <constructor-arg index="0" value="18"/>
    <constructor-arg index="2" value="userDao"/>
</bean>
------------------------------------------------------------------------
//触发setter方法进行注入
<bean id="MovieFinder" class="com.june.ioc_02.MovieFinder"/>
<bean id="SimpleMovieLister" class="com.june.ioc_02.SimpleMovieLister">
        <!--
            name ->属性名setter方法的 去set和首字母小写的值！ 调用set方法的名
                setMovieFinder   -> movieFinder

            value|ref : value直接属性值  ref="其他bean id
                      -->
        <property name="movieName" value="消失的他" />
        <property name="movieFinder" ref="MovieFinder"/>
    </bean>
```

######                  如何创建ioc容器并且读取配置文件

```
@Test
public void getBeanFromIoc(){
    //创建ioc容器对象
    ClassPathXmlApplicationContext applicationContext = new ClassPathXmlApplicationContext();
    applicationContext.setConfigLocation("spring-03.xml");
    applicationContext.refresh();

    //读取容器中的组件
        //方法1:直接根据beanId获取即可
    HappyComponent happyComponent = (HappyComponent) applicationContext.getBean("HappyComponent");

        //方法2:根据beanId,同时指定bean的类性Class
    HappyComponent happyComponent1 = applicationContext.getBean("HappyComponent", HappyComponent.class);

        //方法3:直接获取类型获取
        //TODO:根据Bean的类型获取，同一类型,在ioc容器中只能有一个bean!!!
        //TODO:如果ioc容器中存在多个同类型的Bean会出现:NoUnique...Exception
        //TODO:ioc配置一定是实现类，但是可以根据接口类型获取值! getBean(类型);instanceof ioc
    HappyComponent happyComponent2 = applicationContext.getBean(HappyComponent.class);

    happyComponent1.doWork();
}
```

######                     周期方法的配置

```
package com.june.ioc_04;

public class JavaBean {
    /**
     * 必须是public 必须是 void 返回值 必须是无参
     * 命名随意！
     * 初始化方法 ---->初始化业务逻辑
     *
     * */
    public void inint(){
        System.out.println("JavaBean.init");
    }

    /**
     * 销毁方法
     * */
    public void clear(){
        System.out.println("JavaBean.clear");
    }
}
--------------------------------------------------------------------------------------
<!--  init-method = "初始化方法"
          destroy-method = "销毁方法名"
          spring ioc 容器 就会在对应的时间节点回调对应的方法,我们可以在其中些对应的业务的即可
            -->
    <bean id="javaBean" class="com.june.ioc_04.JavaBean" init-method="inint" destroy-method="clear"/>
```

###### 		FactoryBean的使用场景

```
package com.june.ioc_05;

public class JavaBean {
    private String name;

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }
}
package com.june.ioc_05;
/*
* 制造JavaBean的工厂bean对象
*
* 步骤 1.实现FactoryBean接口<返回值泛型>
* */

import org.springframework.beans.factory.FactoryBean;

public class JavaBeanFactoryBean implements FactoryBean<JavaBean> {
    private String value;
    public void setValue(String value) {
        this.value = value;
    }
    @Override
    public JavaBean getObject() throws Exception {
        //使用你自己的方式实例化对象就可以了
        JavaBean javaBean = new JavaBean();
        javaBean.setName(value);
        return javaBean;
    }

    @Override
    public Class<?> getObjectType() {
        return JavaBean.class;
    }
}
FactoryBean使用场景
		a.代理类的创建
		b.第三方框架的整合
		c.复杂对象实例化等
		
		FactoryBean接口是SpringIoc容器实例化逻辑的可插拔性点
		用于配置复杂的Bean对象，可以将创建存储在FactoryBean的getObject方法
		/*FactoryBean 就是利用对象进行封装到方法来进行造对象*/
----------------------------------------------------------------------------
<!--
    id ->getObject 方法返回对象的标识
    Class ->factoryBean标准化工厂类
-->
    <bean id="javaBean" class="com.june.ioc_05.JavaBeanFactoryBean">
        <!--   此位置的属性:JavaBean工厂类配置 而不是getObject方法-->
        <property name="value" value="二狗子"/>
    </bean>
----------------------------------------------------------------------------
@Test
    public void test_05(){
        //1.创建ioc容器 就会创建组件对象的实例化 --->init
        ClassPathXmlApplicationContext applicationContext = new ClassPathXmlApplicationContext("spring-05.xml");

        //2.读取组件
        JavaBean javaBean = applicationContext.getBean("javaBean", JavaBean.class);

        System.out.println(javaBean);
        //TODO:FactoryBean 工厂也会加入到ioc容器
        Object bean = applicationContext.getBean("javaBean");
        System.out.println(bean);
        //3.关闭ioc
        applicationContext.close();
    }
```

> xml配置ioc方法总结:
>
> ​	1.配置ioc
>
> ​	2.配置di
>
> ​		构造器配置
>
> ​		Setter配置
>
> ​        3.创建和使用SpringIoc容器
>
> ​	4.高级特性
>
> ​		周期方法配置
>
> ​                组件作用配置
>
> ​                FactoryBean使用场景
>
> ​	xml配置 ioc 方式问题总结
>
> ​		1.注入的属性必须添加Setter方法,代码结构乱
>
> ​		2.配置文件和Java代码分离,编写不是很方便
>
> ​                3.XML配置文件效率低

##### 		<2>注解配置Ioc

| @Component      | 该注解用于描述Spring中Bean,他是一个泛化概念，表示容器中的一个组件(Bean),并且可以用在应用的任何层次，例Service层，Dao层等 |
| :-------------- | ------------------------------------------------------------ |
| **@Repository** | **该注解用于将数据访问层的标识**                             |
| **@Service**    | **该注解用于业务层Service**                                  |
| **@Controller** | **该注解用于控制层Controller**                               |

```
---------------CommonComponent-------------------------------
package com.june.ioc_01.components;

import org.springframework.stereotype.Component;

/**
 * projectName: com.atguigu.components
 *
 * description: 普通的组件
 *
 * 1.标记注解
 * 2.配置指定包
 */
@Component //<bean id="common...." class = "..."/>
public class CommonComponent {

}
---------------Controller--------------------------------------
package com.june.ioc_01.Controller;

import org.springframework.stereotype.Controller;

/**
 * projectName: com.atguigu.components
 *
 * description: controller类型组件
 */
@Controller //默认的id xxxController类的首字母小写
/*
* @Controller("ergou")
* @Controller(value = "ergou")
* */
public class XxxController {
}
---------------Dao-----------------------------------------------
package com.june.ioc_01.Dao;

import org.springframework.stereotype.Repository;

/**
 * projectName: com.atguigu.components
 *
 * description: dao类型组件
 */
@Repository
public class XxxDao {
}
---------------Service--------------------------------------------
package com.june.ioc_01.Service;

import org.springframework.stereotype.Service;

/**
 * projectName: com.atguigu.components
 *
 * description: service类型组件
 */
@Service
public class XxxService {
}
-------------------------------------------------------------------
@Controller @Service @Repository 这三个注解只是在@Component注解上起了三个新的名字

通常在xml配置方式中 想要声明类的信息需要<bean id="" class="类在的位置"/>

使用@Controller @Service @Repository 时默认id = 类名首字母小写

@Controller("userController") / @Controller(value = "userController")注 首字母小写

通常在xml配置方式中 想要声明di需要<property name="movieFinder" ref="MovieFinder"/>

使用@Autowired 依赖注入其他组件
	在自动装配的过程中国会出现三种情况
	1.ioc容器种有一个UserSerivce对应的实现类
		直接使用
	2.ioc容器中没有对应的实现类
		使用注解时至少要有一个bean组件 否则报错
		解决方案: @Autowired(required = flase)能装就装,后期开发中要进行配置
	3.ioc容器中有多个组件(在di注入时有多个接口实现类不知道装哪个了)
		解决方案1:指定名
		@Autowired
		private UserService [userService]在这个位置指定你要装配的组件(注 首字母小写)
		解决方案2:使用@Qualifier(value = "userServiceImpl")
		使用该注解获取bean-id,不能单独使用要配合AutoWired
		[可以注解使用 @Resource(name = "userSerivceImpl")指定 bean-id]
@Value 用于给属性直接赋值,通常用于读取配置文件信息 格式:@Value($key:vlaue)
```

​		以上内容是使用注解的方式配置 ioc + di  , 需要额外在spring.xml配置

```
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!--
    1.普通配置包扫描
        base-package指定ioc容器去哪些包下查找注解类
        指定包，相当于指定子包内的所有类
    -->
    <context:component-scan base-package="com.june"/>

    <!--
    2.指定包，但是排除注解
    -->
<!--    <context:component-scan base-package="com.june">-->
<!--        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>-->
<!--    </context:component-scan>-->
    <!--
    3.指定包，指定包含注解
    base-package 包下的所有的注解都生效！！！
    use-default-filters 指定包的所有的注解都不生效
    -->
    <context:component-scan base-package="com.june" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
    </context:component-scan>
</beans>
```

​		创建与使用容器与xml方法一致

###### 		周期配置方法及作用域配置

```
package com.june.ioc_02;

import org.springframework.beans.factory.config.ConfigurableBeanFactory;
import org.springframework.context.annotation.Scope;
import org.springframework.stereotype.Component;

import javax.annotation.PostConstruct;
import javax.annotation.PreDestroy;

@Scope(scopeName = ConfigurableBeanFactory.SCOPE_SINGLETON) //单例默认值
// ConfigurableBeanFactory.SCOPE_SINGLETON 多例
@Component
public class javaBean {


    //周期方法
    @PostConstruct
    public void init(){
        System.out.println("初始化");
    }
    @PreDestroy
    public void destroy(){
        System.out.println("销毁");
    }
}
```

##### 		<3>配置类配置ioc

​			JavaConfiguration配置类来代替 xml配置文件

​			1.包扫描

​			2.引用外部配置文件

​			3.声明第三方依赖bean组件

```
package com.june.javaConfiguration;

import com.alibaba.druid.pool.DruidDataSource;
import org.springframework.beans.factory.annotation.Configurable;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.beans.factory.config.ConfigurableBeanFactory;
import org.springframework.context.annotation.*;
import org.springframework.jdbc.core.JdbcTemplate;

import javax.sql.DataSource;

//声明配置类
@Configuration
//包扫描
@ComponentScan({"com.june.ioc_01","com.june.ioc_02"})
//引入外部配置文件
@PropertySource("classpath:jdbc.properties")
public class JavaConfiguration {
    @Value("${url}")
    private String url;
    @Value("${driver}")
    private String driver;
    @Value("${username}")
    private String username;
    @Value("${password}")
    private String password;
//引入第三方组件
    /*
    * 问题1:beanName的问题
    *       默认:方法名
    *       指定： name/value属性起名字 覆盖方法名
    * 问题2:周期方法如何指定
    *       原有注解方案:PostConstruct+PreDestroy 注解指定
    *       bean属性指定 initMethod / destroyMethod 指定
    * 问题3:作用域
    *       和以前还是一样的
    * 问题4:如何引用其他的ioc主键
    * */
    @Scope(scopeName = ConfigurableBeanFactory.SCOPE_SINGLETON)
    @Bean(name="ergouzi",initMethod = "",destroyMethod = "")
    public DruidDataSource dataSource(){
        //实现具体的实例化过期
        DruidDataSource druidDataSource = new DruidDataSource();
        druidDataSource.setUrl(url);
        druidDataSource.setDriverClassName(driver);
        druidDataSource.setUsername(username);
        druidDataSource.setPassword(password);

        return druidDataSource;
    }
    @Bean
    public JdbcTemplate jdbcTemplate(){
        JdbcTemplate jdbcTemplate = new JdbcTemplate();
        //需要DataSource,需要oc容器的其他组件
        //方案1:如果其他组件也是@Bean方法可以直接调用
        jdbcTemplate.setDataSource(dataSource());
        return jdbcTemplate;
    }
    @Bean
    public JdbcTemplate jdbcTemplate1(DataSource dataSource){
        JdbcTemplate jdbcTemplate = new JdbcTemplate();
        //需要DataSource,需要oc容器的其他组件
        //方案2:形参列表声明想要的组件类，可以是一个也可以多个！ ioc会自动注入
        //形参变量注入，必须要有 DataSouce组件 ,如果没有就抛异常
        //如果有多个:可以使用形参名 DataSource <dataSource>来指定哪个DataSource
        jdbcTemplate.setDataSource(dataSource());
        return jdbcTemplate;
    }
}
-----------------------------------------------------------------------------------
 @Test
    public void test(){
        //1.创建ioc容器
            //方式1:
        AnnotationConfigApplicationContext annotationConfigApplicationContext =
                new AnnotationConfigApplicationContext(JavaConfiguration.class);
            //方式2:
        AnnotationConfigApplicationContext annotationConfigApplicationContext1 = new AnnotationConfigApplicationContext();
        annotationConfigApplicationContext1.register(JavaConfiguration.class);
        annotationConfigApplicationContext1.refresh();
        //2.获取bean
        StudentService bean = annotationConfigApplicationContext.getBean(StudentService.class);

        System.out.println(bean);
    }
```

##### 		<4>SpringTest测试环境

```
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-test</artifactId>
    <version>6.0.6</version>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter-api</artifactId>
    <version>5.3.1</version>
</dependency>
```

```
@SpringJUnitConfig(value = JavaConfig.class)
// locations = 指定配置文件xml
// value = 指定配置类
public class SpringIocTest {
    @Autowired
    private A a;
    @Autowired
    private B b;
    @Test
    public void test() {
        //创建容器

        //获取容器

        //使用组件
    }
}	
```

#####                       SpringAop

​	依赖：spring-context / spring-aspects

###### 	Aop八个核心关键词

![image-20260908164923571](C:\Users\30554\AppData\Roaming\Typora\typora-user-images\image-20260908164923571.png)

###### 	如何使用aop：

​	1.导入依赖 

```
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-context</artifactId>
    <version>6.0.6</version>
</dependency>

<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-aspects</artifactId>
    <version>6.0.6</version>
</dependency>
```

​	2.编写核心业务代码

```
package com.june.serivce.Impl;

import com.june.serivce.Calculator;
import org.springframework.stereotype.Component;

/**
 * 实现计算接口,单纯添加 + - * / 实现! 掺杂其他功能!
 *
 *
 * aop-只针对ioc容器对象 - 创建代理对象 -> 代理对象存储到ioc容器
 */
@Component
public class CalculatorPureImpl implements Calculator {
    
    @Override
    public int add(int i, int j) {

        int result = i + j;

        return result;

    }
    
    @Override
    public int sub(int i, int j) {
    
        int result = i - j;
    
        return result;
    }
    
    @Override
    public int mul(int i, int j) {
    
        int result = i * j;
    
        return result;
    }
    
    @Override
    public int div(int i, int j) {
    
        int result = i / j;
    
        return result;
    }
}
```

​	3.编写ioc的配置类和文件即可

```
package com.june.Config;

import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.EnableAspectJAutoProxy;

@Configuration
@ComponentScan("com.june")
@EnableAspectJAutoProxy //开启aspectj的注解(aop的使用)
public class JavaConfig {
}
```

​	4.测试环境

```
package com.june;

import com.june.Config.JavaConfig;
import com.june.serivce.Calculator;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.junit.jupiter.SpringJUnitConfig;

@SpringJUnitConfig(value = JavaConfig.class)
public class SpringAopTest {

    //aop - 代理 - jdk - 接口 - 代理类 - 代理对象和目标对象 ->必须使用接口
    //aop - ioc - 代理对象
    @Autowired
    private Calculator calculator;

    @Test
    public void test() {
        int add = calculator.add(1, 1);

        System.out.println("add=" + add);
    }
}
```

​	5.增强类(存储横切关注点的代码)

​	6.增强类的配置(切入点,切点指定,切面配置)

```
package com.june.advice;


import org.aspectj.lang.annotation.*;
import org.springframework.core.annotation.Order;
import org.springframework.stereotype.Component;

/*
* 增强类
*
* 1.定义方法存储增强代码
*   具体定义几个方法，根据插入的位置决定
* 2.使用注解配置 指定插入目标方法的位置
*   前置  @Before
*   后置  @AfterReturning
*   异常  @AfterThrowing
*   最后  @After
*   环绕  @Around
* 3.配置切点表达式[选择插入的方法   切点]
*
* 4.加入ioc容器 @Componet
*   配置切面 @Aspect = 切点 + 增强
*
* spring aop 重点 配置 -> jdk,cglib
*
* 5.开启aspect注解的支持@EnableAspectJAutoProxy
* */
@Component
@Aspect
@Order(20)
public class LogAdvice {

    /*
    *
    *TODO:切点表达式
    *   固定语法 execution(1 2 3.4.5(6))
    *   1.访问修饰符
    *       public/private
    *   2.方法的返回参数类型
    *       String int void
    *       如果不考虑访问修饰符和返回值！ 这两位整合成一起写 *
    *       如果要是不考虑，必须两个都不考虑！都不能出现
    *   3.包的位置
    *       具体包:com.june.service.Impl
    *       单层模糊：com.june.service.* 单层模糊
    *       多层模糊：com..impl ..任意层的模糊
    *      找所有impl包:com..impl 不能写..impl 写成 *..impl
    *   4.类的名称
    *       具体 UserImpl
    *       模糊:*
    *       部分模糊 *Impl
    *   5方法名 语法和类名一致
    *   6.(6)形参数类别
    *      没有参数:()
    *      有具体参数: (String) (String,int)
    *       模糊参数:(..) 有没有参数都可以，有多个也可以！
    *       部分模糊:(String..) String 后面有没有无所谓
    *              (..int) 最后一个参数是int
    *              (String..int)前后参数,中间无所谓
    *
    *   小练习
    *   public int xx.xx.jj.*(int)
    *   * xx.xx.jj.*(String..)
    *   * *..*.*()
    *   * com..*.*(..int)
    *   private int com.june.Servie*.*()
    * */

    /*
    * TODO:切点表达式的提取和复用
    *   1.当类中提取
    *     定义一个空方法
    *     注解 @Pointcut
    *     增强注解中引入切点表达式的方法即可
    *   2.创建一个存储切点的类
    *       单独维护切点表达式
    *       其他类的切点方法 类的全限定符合.方法名
    * */

    @Pointcut("execution(* com.june.serivce.Impl.*.*(..))")
    public void pc() {}


    @Before("pc()")---------------------------------------------------指定目标方法的使用位置[利用切点表达式来指定]
    public void start(){-------------------------------------------增强方法
        System.out.println("方式开始了");
    }
    @After("com.june.pointCut.MyPointCut.mypc()")----------------------------指定目标方法的使用位置
    public void after(){-------------------------------------------增强方法
        System.out.println("方法结束了");
    }
    @AfterThrowing("execution(* com.june.serivce.Impl.*.*(..))")-------------指定目标方法的使用位置
    public void error(){-------------------------------------------增强方法
        System.out.println("方法报错了");
    }

}
```

​	7.开启aop的配置

###### 	增强方法中获取目标方法信息

​		1.获取目标方法中的信息(方法,参数,访问修饰符,所属的类的信息)

```
@Before("execution(* com..Impl.*.*(..))")
public void before(JoinPoint joinPoint){
    //1.获取方法所属的类的信息
    String simpleName = joinPoint.getTarget().getClass().getSimpleName();
    //2.获取方法名称
    Signature signature = joinPoint.getSignature();
    //3.获取参数列表
    Object[] args = joinPoint.getArgs();
    //4.获取访问权限
    int modifiers = joinPoint.getSignature().getModifiers();
    String s = Modifier.toString(modifiers);
}
```

​		2.返回结果 @AfterReturning

```
@AfterReturning(value = "execution(* com..Impl.*.*(..))",returning = "result")
public void afterReturning(JoinPoint joinPoint,Object result){

}
```

​		3.接收异常信息 @AfterTrowing

```
@AfterThrowing(value = "execution(* com..Impl.*.*(..))",throwing = "throwable")
public void afterThrowing(JoinPoint joinPoint,Throwable throwable){

}
```

​	多个advice类 可以设置优先级使用@Order 值小级高

###### 	环绕

```
package com.june.advice;

import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.Around;
import org.aspectj.lang.annotation.Aspect;
import org.springframework.stereotype.Component;

//@Component
//@Aspect
public class TxAroundAdvice {
    /*
    * 环绕通知，需要你在通知中，定义目标方法的执行
    *
    * 目标方法的返回值
    *
    * */

    @Around("com.june.pointCut.MyPointCut.pc()")
    public Object transcation(ProceedingJoinPoint proceedingJoinPoint) {

        //保证目标方法被执行即可
        Object[] args = proceedingJoinPoint.getArgs();
        Object result = null;
        try {
            //增强代码 -> beafore
            System.out.println("开启事务");

            result = proceedingJoinPoint.proceed(args);

            System.out.println("结束事务");
        } catch (Throwable e) {

            System.out.println("结束回滚");

            throw new RuntimeException(e);
        }
        return result;
    }
}
绑定核心代码后 使用环绕就不用分开写@Before @After了
```

#####                       SpringTx

###### SpringTx使用流程

​	依赖:org.springframework spring-tx 6.0.6

​	1.在JavaConfig的配置类中 加入第三方类 TransactionManager

​	    设置 DataSource 数据连接池

​	2.在JavaConfig的配置类中声明注解@EnableTransactionManagement

​	3.在Service层中，类 | 方法 上加入 @Transactional 即添加完成事务

> 由于Mysql知识懈怠仅需使用

#####                       SpringMVC

###### 	Web项目创建方式

​		1.创建一个空项目

​		2.在pom.xml中将<packing>设置为pom

​		3.删除src目录

​		4.在pom.xml中加入依赖

```
<dependency>
    <groupId>jakarta.platform</groupId>
    <artifactId>jakarta.jakartaee-web-api</artifactId>
    <version>${servlet.api}</version>
    <scope>provided</scope>
</dependency>
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-webmvc</artifactId>
    <version>${spring.version}</version>
 </dependency>
 <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.15.0</version>
        </dependency>
```

###### 	SpringMvc接收参数

​		1.访问路径设置

​			@RequsetMapping("/[写不写都可以]")

​			<1>精确地址[一个|多个] /user/login {"地址1","地址2"}

​			<2>支持模糊 * 任何一个字符串   |  **任意层任意字符串

​				/user/*   -> /user/a   /user/aaa

​				/user/**  ->  /user/a   /user/a/a/a/a

​		        <3>类上与方法上添加@requestMapping的区别

​				类上提取通用的访问地址

​				方法上是具体的handler地址

​				访问:类地址  + 方法地址 即可

​			<4>请求方式

​				客户端 -> http(get | post |put | delete)

​				默认情况下 @requsetMapping("login") 任何方式都可以访问

​				指定请求方式 method = {RequsetMethod.GET,RequestMethod.post}

​				不符合请求方式:405异常

​			<5>注解指定

​				指定 @GetMapping 等于 method = {RequsetMethod.GET}

​		2.Parm与JSON的区别

​	 		Parm  key=vlaue &key = value

​				Param适用于单一数据的传递

​			Json{key:value , key:vlaue}

​				Json适用于更复杂的数据结构传递

​		3.Param四种接收方式

​			<1>直接接收

​				/param/data?name=root & age = 18  ----->1.名称相同 2.可以不传递  不报错

```
@RequestMapping("data")
@ResponseBody
public String data(String name,int age){
    System.out.println("name" + name + "," + "age" + age);
    return "name" + name+","+"age" + age;
}
```

​			<2>注解指定

​				/param/data1?account=root&page=1

```
@RequsetParam -> 形参类别 指定请求参数名  或 是否必须传递
@RequsetParam(value = "指定请求参数名 如果形参名与请求参数名一致可以省略"
			  required = flase 前后是否必须传递此值 ,默认是必须的 不传汇报400异常
			  deflautVlaue = "1"当非必须传递 可以设置默认值
			)
@RequsetParam 如果不需要指定则根据形参名接收
----------------------------------------------------------------------------
@GetMapping("data1")
@ResponseBody
public String data1(@RequestParam("account") String user,
                    @RequestParam(required = false,defaultValue = "1") int page){
    System.out.println("user" + user + "," + "page" + page);
    return user+":"+page;
}
```

​			<3>特殊值

​				// param/data?hbs=吃&hbs=玩

```
@GetMapping("data2")
@ResponseBody
public String data2(@RequestParam List<String> hbs){
    hbs.forEach(System.out::println);
    return "ok";
}
```

​			<4>使用实体对象接收值

​				//param/data?name=二狗子&age = 18

```
@RequestMapping("data3")
@ResponseBody
public String data3(User user){
    System.out.println("user=" + user);
    return user.toString();
}
```

​		4.动态路径接收

```
/path/账号/密码
接收路径参数 需添加 @PathVariable 与 RequsetParam 使用类似
	如果是(String account , String password)  --->只适用于Param
------------------------------------------------------------------
@RequestMapping("{account}/{password}")
public String path(@PathVariable String account,@PathVariable String password) {
    System.out.println(account + ":" + password);
    return account + ":" + password;
}
```

​		5.Json参数接收

```
前端请求 Post {name:xxxxxxxx , age:xxxxxxxxxx , gender:Xxxxx}
前端 -> json -> 不支持数据类型
原因:Java原始的api,只支持路径参数 和 Param , Json是前端格式
	1.导入Json依赖
	2.配置handlerAdpater   ----->  
	直接在Mvc配置类使用@EnableWebMvc -->代替了handlerMapping , handlerAdpat
---------------------------------------------------
@EnableWebMvc   //handerlerAdapter转换器
@RequestMapping("json")
@Controller
@ResponseBody
public class JsonController {
    @PostMapping("data")
    public String data(@RequestBody Person person){
        System.out.println("person=" + person);
        return person.toString();
    }
}
```

​		4.Cookie 与 header

```
@Controller
@RequestMapping("cookie")
@ResponseBody
public class CookieController {

    @RequestMapping("data")
    public String data(@CookieValue(value = "cookieName") String value){
        System.out.println("value" + value);
        return value;
    }

    @GetMapping("save")
    public String save(HttpServletResponse response){
        Cookie cookie = new Cookie("cookieName", "root");
        response.addCookie(cookie);
        return "ok";
    }
}
------------------------------------------------------------------
@Controller
@RequestMapping("header")
@ResponseBody
public class HeaderController {

    @GetMapping("data")
    public String data(@RequestHeader("Host") String host){
        System.out.println("Host" + host);
        return host;
    }
}
```

​		5.api

```
package com.june.api;

import jakarta.servlet.ServletContext;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import jakarta.servlet.http.HttpSession;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;

@Controller
public class ApiController {

    @Autowired
    private ServletContext servletContext;

    public void data(HttpServletResponse response,
                     HttpServletRequest request,
                     HttpSession session) {

        //正常使用原生对象
        //ServletContext [1.最大的配置文件 2.全局最大共享域 3.核心realPath]
        //方案1: req 获取sission
        ServletContext servletContext = request.getServletContext();
        ServletContext servletContext1 = session.getServletContext();
        //方案2; ServletContext 会自动装入到ioc容器
    }
}
```

![image-20260912225700070](C:\Users\30554\AppData\Roaming\Typora\typora-user-images\image-20260912225700070.png)

###### SpringMvc响应数据

​	1.如何使用摄图解析器?

​		<1>在webapp/web-inf下创建jsp文件

​		<2>在java配置中使用@EnableWebMvc

​		<3>实现WebMvcConfigurer接口，重写configureViewResolvers方法,利用registry.jsp快速添加后缀

```
//视图解析器
@Override
public void configureViewResolvers(ViewResolverRegistry registry) {
    //快速添加前后缀
    registry.jsp("/WEB-INF/views/", ".jsp");

    //handler ->index
}
```

​		<4>配置SpringMvcInit

​		<5>创建Controllerl类

```
/**
 * TODO:快速查找视图
 *  1.方法返回值是字符串类型
 *  2.不能添加@ResponseBody,直接返回给浏览器
 *  3.返回值 对应Jsp文件名
 * */
@GetMapping("index")
public String index(HttpServletRequest request) {
    request.setAttribute("data","hello jsp");
    System.out.println("JspController.class");
    return "index";
}
```

​	2.转发与重定向

```
/**
 * 转发:只能是项目下的资源
 *  1.方法的返回值写成字符串
 *  2.不能添加responseBody注解
 *  3.返回的字符串 forword: /转发地址
 *
 * */
//转发
@GetMapping("forword")
public String forword() {
    System.out.println("JspController.forward");
    return "forward:/jsp/index";
}
```

```
/**
 * 重定向:
 *  1.方法返回值写成字符串类型
 *  2.不能添加responseBody注解
 *  3.返回字符串前面 redirect:/重定向地址
 * */
//重定向
@GetMapping("redirect")
public String redirect() {
    System.out.println("JspController.redirect");
    return "redirect:/jsp/index";
}
```

```
/**
 * 路径细节:[不使用springmvc request response]
 *  转发是项目下的资源跳转  如:项目下的地址 /jsp/index
 *  重定向项目下的资源可以是项目外的地址 重定向属于二次请求
 *          路径: 项目下的地址 全地址 /springMvc/jsp/index
 *
 *  使用springmvc路径语法
 *      "forword:路径 | redirect 路径"  重定向，资源的地址也不需要写项目的根路径!  /jsp/index
 *
 *      转发和重定向的地址都一样了
 * */
```

​	3.json数据的响应  

​	使用@ResponseBody 即可响应Json数据

​	*但是增加后, 查找视图,转发，重定向都不会生效了*

​	使用Json记得在java配置类中配置*@EnableWebMvc* 与 json依赖

​        *@RestController = @Controller + @ResponseBody* 

​	4.静态资源处理

​	localhost:8080/images/testImage.png 访问不到图片？

​	开启静态资源查找,在Java配置类中实现WebMvcConfigurer接口重写	configureDefaultServletHandling方法

```
//开启静态资源查找
//dispatcherServlet -> handlerMapping找有没有对应的handler
@Override
public void configureDefaultServletHandling(DefaultServletHandlerConfigurer configurer) {
    configurer.enable();
}
```

###### RestFul

###### Spring其他扩展

​	全局异常处理机制

#### *Mybatis*

#####  依赖

```
<dependency>
    <groupId>org.mybatis</groupId>
    <artifactId>mybatis</artifactId>
    <version>3.5.11</version>
</dependency>
```

```
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.25</version>
</dependency>
```

##### 使用方法

​	1.创建Pojo

​	2.写好xxxMapper 接口

​	3.创建xxxxMapper.xml

​	4.使用mybatis_config.xml

​	5.创建测试方法

​		a.读取外部配置文件

​		b.创建sqlSessionFactory

​		c.根据sqlSessionFactory创建sqlSession(每次业务创建一个,用完就释放)

​		d.获取接口的代理对象(代理技术)调用代理对象,就会查找mapper接口方法

​		e.提交事务和释放资源

> 源码见../code/SSM-Spring

##### Mybatis的基本使用

###### 	数据的输入

```
//单一数据的输入
//根据id查询员工对象
Employee queryById(Integer id);
------------------------------------------------------
<select id="queryById" resultType="com.june.Employee">

        select emp_id empId,emp_name empName, emp_salary empSalary from
        t_emp where emp_id = #{id}

    </select>
//传入单个简单类型的Key随便写
//根据id删除员工信息
int deleteById(Integer id);
------------------------------------------------------
delete id="deleteById" >
        delete from t_emp where emp_id = #{ ergouzi }
    </delete>
```

```
//传入的是一个实体对象
//插入员工数据 【实体对象】
int insert(Employee employee);
------------------------------------------------------
<insert id="insertEmp">
        insert into t_emp(emp_name , emp_salary ) values (#{empName},#{empSalary})
    </insert>
```

```
//传入多个简单类型的数据
	方案1:注解指定
//根据员工姓名和工资查询员工信息
List<Employee> queryByNameAndSalary(@Param("a") String name,
                                    @Param("b") Double salary);
------------------------------------------------------------------
<select id="queryByNameAndSalary" resultType="com.june.Employee">
        select emp_id empId , emp_name empName ,emp_salary empSalary
        from t_emp where emp_name = #{a} emp_salary = #{b}
    </select>
    方案2:mybatis的默认机制
    	从左到右一次为arg0,arg1
    	<from t_emp where emp_name = #{arg0} emp_salary = #{arg1}>
```

```
//使用Map
//插入员工数据,传入的是一个map(name=员工名字,salary=员工的薪水)
int insertEmpMap(Map data);
---------------------------------------------------------------
<insert id="insertEmpMap">
        insert into t_emp(emp_name , emp_salary ) values (#{name},#{salary})
    </insert>
```

###### 	数据的输出

​	1.DML(增加,删除,修改) 无需定义

```
<delete id="deleteById">
    delete from t_emp where emp_id = #{id}
</delete>
```

​	2.返回单个简单类型如何指定 resultType的写法！  返回值的数据类型!!!

​		resultType语法：类的全限定符号   别名简称

​					       **mybatis给我们提供了72种默认的别名这些都是常用的java数据类型**

​							基本数据类型   int double  -   _int     _double

​							包装数据类型    Interger  Double   -  integer  double

​							集合容器  Map  List  HashMap  -    map list hashmap   

​						**如果需要提供自己自定义的或者写类的全限定符号 如何设置**

​						在mybatis - config 配置文件settings下方 

​						<typAliases>

​						单独定义:

​						 <typeAlias type="com.june.pojo.Employee" alias="ergouzhi">

​						批量将包下的类给别名,别名就是类的首字母小写

​						<package name="com.june.pojo">

​						<typAliases/>

​						

​	3.返回单个实体类型时 要求列名于属性名要一致,这样才能进行实体类属性映射

​	   当Java为驼峰命名，mysql是_命名方法时 在mybatis配置中设置mapUnderscoreToCamelCase为True

```
<settings>
    <!--  开启mybatis的日志输出功能      -->
    <setting name="logImpl" value="STDOUT_LOGGING"/>
    <!--  开启驼峰式自动映射      -->
    <setting name="mapUnderscoreToCamelCase" value="true"/>
</settings>
```

​	4.返回map   key = 查询的列名, value  = 查询的值

​	5.返回的是集合类型

​		返回值是List集合  resultType不需要指定集合类型,只需要指定泛型即可

```
<select id="queryNamesBySalary" resultType="string">
    select emp_name from t_emp where emp_salary > #{salary}
</select>
--------------------------------------------------------------
List<String> queryNamesBySalary(Double salary);
```

​	 6.主键回显  获取插入数据的主键

​		自增长主键回显mysql   auto_increment	

```
useGeneratedKeys="true" 我们想要数据自动增长的主键值
keyColumn="empid" 主键列的值!!!
keyProperty="empId"  接收主键列值的属性!!!
------------------------------------------
<insert id="intsertEmp" useGeneratedKeys="true" keyColumn="empid" keyProperty="empId">
        insert into t_emp (emp_name,emp_salary)
            value(#{empName},#{empSalary})
    </insert>
    这样插入改对象后,在再次调用该对象就有主键id了
```

​		非自增长主键设置(如果mysql没有设置自增长,mybatis可以帮助设置自增长)

```
<insert id="insertEmp">
    <!-- 插入之前,先执行这段 SQL 生成主键 -->
    <selectKey order="BEFORE" resultType="string" keyProperty="tId">
        SELECT REPLACE(UUID(),'-','')
    </selectKey>

    insert into t_emp (t_id, emp_name, emp_salary)
    values (#{tId}, #{empName}, #{empSalary})
</insert>
```

​	7.java类的属性名与mysql列名不一致时

​		a.在sql中起别名

​		b.使用驼峰转换

​		c.resultMap自定义映射(resultMap/)

​		   resultType按照规则自动映射,只能按照是否开启驼峰式映射,自己映射属性和列名,只能映射一层

```
 声明resultMap 标签 自己定义映射规则
 id标识 -> slect resultMap="标识"
 type  -> 具体的返回值类型 全限定符和别名 | 集合只写泛型既可以
                   < id 主键映射的关系
                   < result 普通列的映射关系
<resultMap id="tMap" type="teacher">
        <id column="t_id" property="tId"/>
        <result column="t_name" property="tName"/>
    </resultMap>
<select id="queryById" resultMap="tMap">

</select>

```

###### 	多表查询

​		1.多表结果实体类设计小技巧： 对一，属性中包含对方对象

  		 对多，属性中包含对方对象集合

​		   *只有真实发生多表查询时，才需要设计和修改实体类,否者不提取  设计和修改实体类*

​                   *无论多少张表联查，实体类的设计都需要两两考虑*

​                   *在查询映射的时候，只需要关注本次查询相关的属性！ 例如:查询订单和对应的客户，就不需要关注*

​                   *客户中的订单集合*！

​		2.一个表中嵌套一个对象(一个订单对应一个客户)

​	![image-20260910223657664](C:\Users\30554\AppData\Roaming\Typora\typora-user-images\image-20260910223657664.png)

​		3.一个客户对应多个订单

![image-20260910224147872](C:\Users\30554\AppData\Roaming\Typora\typora-user-images\image-20260910224147872.png)

​            只考虑与我业务相关的，至于Order里面的Customer对象就不用写了

###### 	动态语句

​		1.where - if

```
<select id="query" resultType="employee">
    select * from t_emp
        <where>
            <if test="name !=null">
                emp_name = #{name}
            </if>
            <if test="salary !=null and salary &gt; 100">
                and emp_salary = #{salary}
            </if>
        </where>
</select>
```

​		where标签的作用 

​			a.自动去掉where关键字,where中只要有一个if满足就会自动保留

​			b.自动删除多余的or / and

​		2.set - if

```
<update id="update">
    update t_emp
    <set>
        <if test="empName !=null">
            emp_name = #{empName},
        </if>
        <if test="empSalary">
            emp_salary = #{empSalary}
        </if>
    </set>
        where emp_id = #{empId}
</update>
```

​		自动添加set

​		3.trim - if

```
<select id="queryTrim" resultType="employee">
    select * from t_emp
    <trim prefix="where">
        <if test="name !=null">
            emp_name = #{name}
        </if>
        <if test="salary !=null and salary &gt; 100">
            and emp_salary = #{salary}
        </if>
    </trim>
</select>
```

​		只要满足一个就加where

​		4.choose - when

```
<select id="queryChoose" resultType="employee">
    select * from t_emp
    where
    <choose>
        <when test="name! = null">
            emp_name = #{name}
        </when>
        <when test="salary != null">
            and emp_salary = #{salary}
        </when>
        <otherwise>1=1</otherwise>
    </choose>
</select>
```

​		第一个条件满足就用第一个，第二个满足就用第二个

​		5.遍历数据[如果一次发送多条sql语句:jdbc:mysql:///mybatis-example?allowMultiQueries=true]

```
collection = ids
       open 遍历之前要追加的字符串
       close  遍历结束要添加的字符串
       separator 每次遍历的分割符号！ 如果最后一次,不会追加
       item 获取每个遍历项
----------------------------------------------------------
<select id="queryBatch" resultType="employee">
    select * from t_emp
            where emp_id in
            <foreach collection="ids" open="(" separator="," close=")" item="id">
                <!-- 遍历的内容 #{遍历项 item指定的key} -->
                #{id}
            </foreach>
</select>

<delete id=" deleteBatch">
    delete for t_emp where id in
            <foreach collection="ids" open="(" separator="," close=")" item="id">
                #{id}
            </foreach>
</delete>

<insert id="insertBatch">
    insert into t_emp (emp_name,emp_salary) values
        <foreach collection="list" separator="," item="employee">
            (#{employee.empName},#{employee.empSalary})
        </foreach>
</insert>

<update id="updateBatch">
    <foreach collection="list" item="emp">
        update t_emp set emp_name = #{emp.empName} ,emp_salary = #{emp.emp_salary}
        where emp_id = #{emp.empId};
    </foreach>
</update>
```

​		6.Sql片段：利用<sql>标签 提取sql语句复用部分

​    				<include refid="sql标签id"/>代替复用部分

```
<sql id="selectSql">
    select * from t_emp
</sql>
<select id="queryBatch" resultType="employee">
    <include refid="selectSql"/>
    where emp_id in
    <foreach collection="ids" open="(" separator="," close=")" item="id">
        <!-- 遍历的内容 #{遍历项 item指定的key} -->
        #{id}
    </foreach>
</select>
```

###### 	Mapper按包批量扫描

​		如果有多个xxxMapper.xml,可以在resource 文件夹下创建对应结构的Mapper(com/june/mapper)

​		1.xxxMapper.xml  与 xxxMapper接口  名称必须一致

​		2.com.june.mapper   =  resource/com/june/mapper  

​		3.在mybatis配置文件中设置<mappers><package name="com.june.mapper"></mappers>